.DS_Store

# Hugo build artifacts
.hugo_build.lock
public/
# Local Hugo asset-cache entries (the committed cache stays tracked; gitignore only hides NEW hash files a local build generates)
resources/_gen/
